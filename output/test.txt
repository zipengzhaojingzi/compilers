
struct student
{
    char *name;
    int num;
    int age;
    float score;
};

void main() // 修正main函数返回类型
{
    int i, flag, num_140 = 0;
    float sum = 0;
    student sts[2] = {                           // 修正数组初始化语法
                      {"Li ping", 5, 18, 145.0}, // 修正中文引号为英文
                      {"Wang ming", 6, 18, 150.0}};

    if (sts[1].score < 140) // 修正14o为140
        flag = -1;
    else
        flag = 1; // 修正字母l为数字1

    printf("%d ", flag);
}