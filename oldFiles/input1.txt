/* 终极测试用例：覆盖所有词法规则 */

// 1. 基础语法测试
if (x >= 42 && y <= 0 || z != 1) {
    a = (b + c) * d - e / f;
}

// 2. 错误场景测试
num = 3.14;     // 小数点错误
flag = &value;   // 非法运算符


// 3. 边界值测试
max_int = 2147483647;  // 最大整数
empty = ;               // 缺失表达式

// 4. 符号表溢出测试（重复标识符）
var1 = 1;
var2 = 2;
var3 = 3; // ...（假设此处继续添加直到符号表满）
 
 /*
 -------------------------------*/
 
/* 未闭合块注释==============
